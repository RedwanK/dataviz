# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
POSTGRES_DB="dataviz"
POSTGRES_USER="dataviz"
POSTGRES_PASSWORD="dataviz"
POSTGRES_VERSION=16
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=8.0.32&charset=utf8mb4"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:5432/${POSTGRES_DB}?serverVersion=16&charset=utf8
###< doctrine/doctrine-bundle ###


###> symfony/messenger ###
# Messenger default transport via Redis (container `redis`)
MESSENGER_TRANSPORT_DSN=redis://redis:6379/messages
###< symfony/messenger ###

###> symfony/mailer ###
MAILER_DSN=null://null
###< symfony/mailer ###

###> symfony/mercure-bundle ###
# See https://symfony.com/doc/current/mercure.html#configuration
# Dev hub URLS (using local compose mapping)
MERCURE_PORT=8080
MERCURE_URL=http://127.0.0.1:${MERCURE_PORT}/.well-known/mercure
MERCURE_PUBLIC_URL=http://127.0.0.1:${MERCURE_PORT}/.well-known/mercure
# JWT keys used by Mercure hub (publisher/subscriber)
MERCURE_PUBLISHER_JWT_KEY=!ChangeThisMercureHubJWTSecretKey!
MERCURE_SUBSCRIBER_JWT_KEY=!ChangeThisMercureHubJWTSecretKey!
# Keep Symfony bundle secret aligned with publisher key
MERCURE_JWT_SECRET=${MERCURE_PUBLISHER_JWT_KEY}
# CORS origins for the browser app
MERCURE_CORS_ORIGINS=http://127.0.0.1:8000
# Set to ':80' to disable HTTPS in the hub (dev)
MERCURE_SERVER_NAME=:8080
###< symfony/mercure-bundle ###

###> mqtt/broker ###
# MQTT broker configuration
# Port exposed on host and used inside container
MQTT_PORT=1883
# Optional: set to enable authentication (leave empty to allow anonymous in dev)
MQTT_USERNAME=
MQTT_PASSWORD=
###< mqtt/broker ###

###> mqtt/bridge ###
# Hostname of the broker from the bridge container
MQTT_HOST=mqtt
# Topics to subscribe (supports wildcards, comma not needed)
MQTT_BRIDGE_TOPICS=#
###< mqtt/bridge ###
